# @package _global_

# to execute this experiment run:
# python -m optispeech.train experiment=ASEEL

defaults:
  - override /data: ASEEL
  - override /model: optispeech

tags: ["ASEEL", "light", "arabic", "single_speaker", "male", "ar"]

run_name: ASEEL
